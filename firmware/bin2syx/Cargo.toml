[package]
name = "bin2syx"
version = "0.1.0"
edition = "2024"

[dependencies]
embedded-io-adapters = { version = "0.6.2", features = ["std"] }
midi-tools = { version = "0.1.0", path = "../midi-tools" }
