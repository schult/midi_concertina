[package]
name = "midi-tools"
license = "GPL-3.0-only"
edition = "2024"

[dependencies]
circular-buffer = { version = "1.2.0", default-features = false, features = ["embedded-io"] }
embedded-io = "0.6.1"

[dev-dependencies]
hex-literal = "1.0.0"
mockall = "0.13.1"
tokio = { version = "1.48.0", features = ["rt", "macros"] }
